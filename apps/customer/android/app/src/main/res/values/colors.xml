<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Adaptive-icon background: branded lavender (was #210035, a near-black
         dark purple that read as a black square behind the icon on launch). -->
    <color name="ic_launcher_background">#F5F2FF</color>
    <!-- Fixed branded launch background (light lavender, matches the Flutter
         splash) so the pre-Flutter frame is never black in dark mode. -->
    <color name="launch_bg">#F5F2FF</color>
</resources>